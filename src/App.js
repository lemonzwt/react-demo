import React from 'react'
import {
    BrowserRouter as Router
} from 'react-router-dom'
import AppRoot from './AppRoot'
const App = () => (
    <Router>
        <AppRoot />
    </Router>
);
export default App;
